<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <title>📖 Гостевая книга</title>
    <style>
        body {
            background: #000;
            color: #0f0;
            font-family: "Courier New", monospace;
            padding: 2em;
        }
        h1 {
            font-size: 2em;
        }
        form {
            margin-top: 1em;
        }
        textarea {
            width: 100%;
            height: 100px;
            background: #111;
            color: #0f0;
            border: 1px solid #0f0;
            padding: 1em;
            font-family: inherit;
            font-size: 1em;
        }
        input[type="submit"] {
            margin-top: 1em;
            background: #0f0;
            color: #000;
            border: none;
            padding: 0.5em 1em;
            cursor: pointer;
            font-weight: bold;
        }
        .messages {
            margin-top: 2em;
            white-space: pre-wrap;
        }
        .nav {
            margin-top: 2em;
        }
        .nav a {
            color: #0ff;
            text-decoration: none;
            margin-right: 1em;
            border: 1px solid #0ff;
            padding: 0.5em;
        }
        .nav a:hover {
            background-color: #0ff;
            color: #000;
        }
    </style>
</head>
<body>
    <h1>📖 Оставьте бессмысленное сообщение</h1>
    <form method="POST">
        <textarea name="message" required></textarea><br>
        <input type="submit" value="📝 Добавить запись">
    </form>

    <div class="messages">
        {% for msg in messages %}
            <div>{{ msg }}</div>
        {% endfor %}
    </div>

    <div class="nav">
        <a href="/">🏠 На главную</a>
    </div>
</body>
</html>
