<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <title>⚙ Центр Бессмысленного Управления</title>
    <style>
        body {
            background: #000;
            color: #0f0;
            font-family: "Courier New", monospace;
            padding: 2em;
        }
        h1 {
            font-size: 2em;
            margin-bottom: 1em;
        }
        textarea {
            width: 100%;
            height: 400px;
            background: #111;
            color: #0f0;
            border: 1px solid #0f0;
            padding: 1em;
            font-family: inherit;
            font-size: 1em;
        }
        input[type="submit"] {
            margin-top: 1em;
            background: #0f0;
            color: #000;
            border: none;
            padding: 0.5em 1em;
            cursor: pointer;
            font-weight: bold;
        }
        .message {
            margin-top: 1em;
            color: #f0f;
        }
        .nav {
            margin-top: 2em;
        }
        .nav a {
            color: #0ff;
            text-decoration: none;
            margin-right: 1em;
            border: 1px solid #0ff;
            padding: 0.5em;
        }
        .nav a:hover {
            background-color: #0ff;
            color: #000;
        }
    </style>
</head>
<body>
    <h1>🛠 Редактирование Смысла</h1>

    <form method="POST">
        <textarea name="content">{{ content }}</textarea><br>
        <input type="submit" value="💾 Обновить Бессмысленность">
    </form>

    {% if message %}
        <div class="message">{{ message }}</div>
    {% endif %}

    <div class="nav">
        <a href="/">🏠 На главную</a>
        <a href="/logs">📜 Логи</a>
        <a href="/admin/guestbook">📖 Гостевая (админ)</a>
    </div>
</body>
</html>
